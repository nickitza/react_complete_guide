//children is a reserve property name
//props.children special simply outputs whatever is output in between the opening and closing tag in a component
const aux = props => props.children

export default aux